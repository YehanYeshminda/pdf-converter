import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yydev.pdf.converter',
  appName: 'yydev-pdf-converter',
  webDir: 'undefined'
};

export default config;
